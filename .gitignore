# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.netlify
*.local

# Secrets — never commit. API keys live in Cloudflare/Netlify env vars only.
.env
.env.*
*.key
*.pem

# Local audit / analysis artifacts (not part of the site source)
audit-screenshots/
codex-deep-integrity-screenshots/
daynight-dental-audit-handoff-*/
daynight-dental-audit-handoff-*.zip
daynight-dental-*-audit.md
daynight-dental-*-review.md
_verify_*.mjs

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Hostinger secret config (contains the Resend key) — never commit
hostinger/api/_config.php

# Temporary preview gate (contains gate secret) - never commit
hostinger/preview.php

# Local deploy convenience (reads FTP creds) - never commit
hostinger/deploy-local.sh

# Local FTP creds, kept in-project at owner request; gitignored so never committed
hostinger/.deploy.env

# temp: display-font trial artifacts (decide at go-live, then remove)
public/font-trial.html
public/fonts/trial-*.woff2
